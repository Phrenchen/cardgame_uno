import React, {Component} from "react";
import PropTypes from "prop-types";

class PlayerOpenCards extends Component{
    render(){
        return (
            <div>
                
            </div>
        );
    }
}

PlayerOpenCards.propTypes = {
    cards:PropTypes.array.isRequired
}

export default PlayerOpenCards;